# Profile "local" — may tinh ca nhan (IDE / mvn spring-boot:run).
# DB SQL Server VARCHAR + Hibernate nationalized = true gay loi "varchar to NCHAR unsupported".
spring.jpa.properties.hibernate.use_nationalized_character_data=false
